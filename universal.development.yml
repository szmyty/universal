# Docker Compose for development
services:
  api:
    env_file:
      - .env.universal
      - .env.development

  database:
    env_file:
      - .env.universal
      - .env.development

  pgadmin:
    env_file:
      - .env.universal
      - .env.development

  web:
    env_file:
      - .env.universal
      - .env.development
